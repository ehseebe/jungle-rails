<section class="orders-show">

      <% if flash[:notice] %>
    <div class="alert alert-warning"><%= flash[:notice] %></div>
      <% end %>

  <header class="page-header">
    <h1>Order #<%= @order.id %></h1>
  </header>

  <div class="order-summary jumbotron">
    <h4>Thank you for your order <%= current_user.first_name %>! 🌴
    <% if current_user %>
      An order confirmation has been sent to <%= current_user.email %>.</h4>
    <% end %>
  </div>

  <%= render 'order_item', order: @order %> 

    

  <%= link_to "Continue Shopping", root_path, class: 'btn btn-lg btn-info' %>

</section>
